<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.c.practicerecyclerview3">

    <application
        android:name=".HeroApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_ok"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_ok_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticeRecyclerView3">
        <activity android:name=".ui.onboarding.ui.OnboardingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"></activity>
        <activity android:name=".ui.HomeActivity">

        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>